// src/lib/theme/theme.ts
"use client";
import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  // Only add what you absolutely need right now
  palette: {
    mode: "light",
  },
});
